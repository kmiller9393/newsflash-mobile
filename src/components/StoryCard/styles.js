import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    cardContainer: {
        alignItems: 'center',
        height: 250,
        width: '100%'
    },
    image: {
        height: '80%',
        width: '100%'
    }
});

export default styles;