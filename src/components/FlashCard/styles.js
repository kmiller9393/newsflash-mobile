import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    cardContainer: {
        alignItems: 'center',
        height: 40,
        margin: 8,
        width: 40
    },
    image: {
        height: 25,
        width: 25
    }
});

export default styles;