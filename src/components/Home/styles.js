import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    homeContainer: {
        height: '100%',
        width: '100%'
    },
    storyContainer: {
        flex: 1
    }
});

export default styles;